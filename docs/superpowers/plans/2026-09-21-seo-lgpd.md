# SEO, Performance e LGPD (Fase 9) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementar a Fase 9 (SEO, performance e LGPD) do plano mestre — metadados dinâmicos completos, imagens Open Graph, JSON-LD estruturado, `sitemap.xml`/`robots.txt`, ajustes de `next/image` e uma página de privacidade honesta com a arquitetura real do site (nenhum dado pessoal armazenado no servidor).

**Architecture:** Tudo usa convenções nativas do App Router (Next 16) — `generateMetadata`, arquivos `sitemap.ts`/`robots.ts`/`opengraph-image.tsx` — sem nenhuma dependência nova. Nenhuma mudança em `/admin`, Supabase, roteamento ou no fluxo de leads via WhatsApp. Zero pacotes novos: `next/og` (`ImageResponse`) já vem embutido no Next.

**Tech Stack:** Next.js 16 (App Router) + TypeScript + `next/og` (embutido) + os tokens de cor/tipografia já existentes do redesign.

**Spec:** `docs/PLANO-IMPLEMENTACAO.md` §4 "Fase 9 — SEO, performance e LGPD" (linhas 331-344). Leia também `PRODUCT.md` (voz da marca, princípio "sem formulário que desaparece num banco de dados") e `CLAUDE.md` "Decisions already made" (nenhum lead é armazenado hoje).

## Global Constraints

- URL canônica: sempre `SITE.url` (`https://matiasimoveisgo.com.br`, de `src/lib/site.ts`) para toda URL absoluta (sitemap, JSON-LD, OG) — mesmo o domínio próprio ainda não estar apontado (é o mesmo padrão já usado em `metadataBase` no layout raiz). Não usar a URL temporária do Vercel.
- Zero dependências novas no `package.json` — tudo é convenção nativa do Next 16 (`next/og`, arquivos de metadata especiais).
- Comentários novos em português; identificadores de domínio (Imovel/Corretor) em português — ver CLAUDE.md "Domain vocabulary".
- Fora de escopo — não tocar: `src/app/admin/**`, `src/lib/supabase/**`, `src/lib/queries.ts` (só leitura, sem mudar assinaturas), `src/lib/admin/**` (exceto o re-export descrito na Task 1), a lógica de `src/lib/whatsapp.ts`.
- **Nenhum navegador está disponível neste ambiente de execução.** Verificação visual de imagens OG geradas, auditoria Lighthouse e validação no Google Rich Results Test **não podem ser feitas nesta sessão** — cada task marca explicitamente o que foi verificado por inspeção de código/build vs. o que fica pendente de verificação visual humana. Não afirme "Lighthouse ≥ 90" ou "a imagem OG está bonita" sem tê-las visto renderizadas.
- Conteúdo da página de privacidade deve refletir a arquitetura real (verificada nesta sessão): nenhum dado de formulário é armazenado no servidor (formulários só abrem o WhatsApp — ver `src/lib/whatsapp.ts` e `CLAUDE.md` "Forms → WhatsApp, not email/database"); o único dado coletado é analytics agregado e anônimo via Vercel Analytics (adicionado nesta mesma sessão, `src/app/layout.tsx`). Não escrever texto de política de privacidade genérico/copiado que não corresponda a isso.
- Voz da marca (PRODUCT.md): primeira pessoa do plural, tratamento "você", tom acolhedor e direto — nunca corporativo/frio, nunca jurídico-genérico.
- "Texto alternativo nas fotos" (item da Fase 9) já está satisfeito — verificado nesta sessão: `ImovelPhoto` exige a prop `alt` (não opcional) em todo call site, e todos os usos já passam um valor real (título do imóvel, label do slide etc.). Não há task para isso neste plano porque não há nada para corrigir.

---

## File Structure

**Task 1 — fundação (labels compartilhadas):**
- Modify: `src/lib/imovel-kind-categories.ts` — ganha `KIND_OPTIONS`, `KIND_LABELS`, `LEGACY_KIND_LABELS` (movidos de `lib/admin/labels.ts`).
- Modify: `src/lib/admin/labels.ts` — re-exporta os três de `imovel-kind-categories.ts` em vez de defini-los (mantém os 2 call sites do admin funcionando sem mudança).
- Modify: `src/components/imovel/SearchFilterBar.tsx` — troca o import de `KIND_OPTIONS` de `@/lib/admin/labels` para `@/lib/imovel-kind-categories` (corrige uma dependência pré-existente do público em código do admin).

**Tasks 2-4 — ficha do imóvel (metadados + JSON-LD + imagem OG):**
- Modify: `src/app/(site)/imovel/[slug]/page.tsx` — `generateMetadata` completo (Task 2) e `<script type="application/ld+json">` com `RealEstateListing` + `BreadcrumbList` (Task 4).
- Create: `src/app/(site)/imovel/[slug]/opengraph-image.tsx` (Task 3).

**Task 5 — sitemap e robots:**
- Create: `src/app/sitemap.ts`
- Create: `src/app/robots.ts`

**Task 6 — imagem OG estática do site:**
- Create: `src/app/opengraph-image.tsx` (cobre Home e todas as páginas públicas sem imagem própria — a da Task 3 tem prioridade na ficha do imóvel).

**Task 7 — JSON-LD RealEstateAgent:**
- Modify: `src/app/(site)/layout.tsx` — `<script type="application/ld+json">` com dados da empresa (`RealEstateAgent`), presente em toda página pública.

**Task 8 — next/image (sizes/priority/blur):**
- Modify: `src/components/imovel/ImovelGallery.tsx` — adiciona `sizes` que faltava na foto principal.
- Modify: `src/app/(site)/page.tsx` — hero vira import estático (`placeholder="blur"` automático).

**Task 9 — privacidade (LGPD):**
- Create: `src/app/(site)/privacidade/page.tsx`
- Modify: `src/lib/site.ts` — novo link em `FOOTER_LINKS`.
- Modify: `src/components/forms/ContactForm.tsx` e `src/components/forms/SellForm.tsx` — aviso curto acima do botão de envio.

---

## Task 1: Extrair labels de tipo de imóvel para módulo público

**Files:**
- Modify: `src/lib/imovel-kind-categories.ts`
- Modify: `src/lib/admin/labels.ts`
- Modify: `src/components/imovel/SearchFilterBar.tsx:18`

**Interfaces:**
- Produces: `KIND_OPTIONS: { value: ImovelKind; label: string }[]`, `KIND_LABELS: Record<ImovelKind, string>`, `LEGACY_KIND_LABELS: Partial<Record<ImovelKind, string>>` — todos agora exportados de `@/lib/imovel-kind-categories` (mesmos nomes e formatos de antes, só mudou o arquivo de origem).

**Contexto:** `src/components/imovel/SearchFilterBar.tsx` (componente **público**) já importa `KIND_OPTIONS` de `@/lib/admin/labels` — uma dependência do site público em código do admin que já existia antes desta task. A Task 5 (metadados da ficha do imóvel, também pública) vai precisar do mesmo `KIND_LABELS`. Em vez de adicionar uma segunda dependência pública em `lib/admin/`, movemos as três constantes para `src/lib/imovel-kind-categories.ts` — que já é o lugar público-seguro para constantes de "tipo de imóvel" (já tem `KIND_CATEGORIES`/`resolveKindFilter()`) — e corrigimos a dependência pré-existente de quebra.

- [ ] **Step 1: Mover as três constantes para `imovel-kind-categories.ts`**

Conteúdo completo do arquivo (adiciona ao que já existe, não remove nada):

```ts
import type { ImovelKind } from "./types";

export type ImovelKindCategory = "residencial" | "comercial" | "lotes";

/**
 * Agrupamento de ImovelKind em categorias amplas para o filtro "Tipo" do
 * SearchFilterBar (primeira linha de botões: Todos/Residencial/Comercial/
 * Lotes). A segunda linha do filtro usa KIND_OPTIONS diretamente (um botão
 * por tipo específico) — essas categorias só existem como atalho.
 */
export const KIND_CATEGORIES: { value: ImovelKindCategory; label: string; kinds: ImovelKind[] }[] = [
  { value: "residencial", label: "Residencial", kinds: ["casa", "kitnet", "apartamento"] },
  { value: "comercial", label: "Comercial", kinds: ["sala_comercial", "galpao"] },
  { value: "lotes", label: "Lotes", kinds: ["lote"] },
];

/**
 * Resolve o valor bruto do parâmetro `tipo` da URL — pode ser um ImovelKind
 * específico (vindo da segunda linha de botões) ou uma categoria (primeira
 * linha) — no formato que searchImoveis() espera: um kind único, uma lista
 * de kinds (para os filtros .in()), ou undefined ("Todos").
 */
export function resolveKindFilter(tipo?: string): ImovelKind | ImovelKind[] | undefined {
  if (!tipo) return undefined;
  const category = KIND_CATEGORIES.find((c) => c.value === tipo);
  return category ? category.kinds : (tipo as ImovelKind);
}

/**
 * Rótulos em português por tipo de imóvel — usados no filtro de busca
 * público (SearchFilterBar), no admin (formulário e listagem) e nos
 * metadados/SEO da ficha do imóvel. Vive aqui (não em lib/admin/) porque
 * o público depende diretamente disso.
 */
export const KIND_OPTIONS: { value: ImovelKind; label: string }[] = [
  { value: "casa", label: "Casa" },
  { value: "kitnet", label: "Kitnet" },
  { value: "apartamento", label: "Apartamento" },
  { value: "sala_comercial", label: "Sala comercial" },
  { value: "galpao", label: "Galpão" },
  { value: "lote", label: "Lote" },
  { value: "outros", label: "Outros" },
];

/**
 * "sobrado" foi removido das opções selecionáveis (decisão do cliente — esse
 * tipo de imóvel agora entra em "Casa"/"Outros"), mas o enum do banco e uma
 * linha de exemplo já existente (`ref: "4"`) ainda usam esse valor, e não dá
 * para renomear com segurança sem acesso direto ao banco. Mantido só para o
 * ImovelForm mostrar como opção de fallback (quase somente leitura) ao
 * editar essa linha legada específica, em vez do <select> trocar
 * silenciosamente para outro tipo ao salvar.
 */
export const LEGACY_KIND_LABELS: Partial<Record<ImovelKind, string>> = {
  sobrado: "Sobrado (descontinuado)",
};

export const KIND_LABELS: Record<ImovelKind, string> = Object.fromEntries(
  KIND_OPTIONS.map((o) => [o.value, o.label])
) as Record<ImovelKind, string>;
```

- [ ] **Step 2: `lib/admin/labels.ts` passa a re-exportar em vez de definir**

Conteúdo completo do arquivo:

```ts
import type { ImovelStatus } from "@/lib/types";

// KIND_OPTIONS/KIND_LABELS/LEGACY_KIND_LABELS vivem em imovel-kind-categories.ts
// (código público já depende deles — ver SearchFilterBar.tsx) — re-exportados
// aqui só para não quebrar os imports existentes do admin.
export { KIND_OPTIONS, KIND_LABELS, LEGACY_KIND_LABELS } from "@/lib/imovel-kind-categories";

export const STATUS_OPTIONS: { value: ImovelStatus; label: string }[] = [
  { value: "disponivel", label: "Disponível" },
  { value: "em_negociacao", label: "Em negociação" },
  { value: "vendido", label: "Vendido" },
  { value: "alugado", label: "Alugado" },
];

export const STATUS_LABELS: Record<ImovelStatus, string> = Object.fromEntries(
  STATUS_OPTIONS.map((o) => [o.value, o.label])
) as Record<ImovelStatus, string>;
```

- [ ] **Step 3: Corrigir o import em `SearchFilterBar.tsx`**

Linha 18, troque:
```ts
import { KIND_OPTIONS } from "@/lib/admin/labels";
```
por:
```ts
import { KIND_OPTIONS } from "@/lib/imovel-kind-categories";
```
(a linha seguinte, que já importa `KIND_CATEGORIES`/`ImovelKindCategory` do mesmo arquivo, fica igual — considere juntar os dois imports numa linha só já que agora vêm do mesmo módulo.)

- [ ] **Step 4: Verificar**

```bash
npm run build
```
Turbopack deve compilar e o TypeScript deve passar sem erro (o build vai falhar mais adiante em "Collecting page data" para `/imovel/[slug]` só se o projeto Supabase estiver pausado — não relacionado a esta task; confirme que a falha, se houver, é exatamente essa e não um erro de import/tipo). Confirme com grep que não sobrou nenhum import de `KIND_OPTIONS`/`KIND_LABELS`/`LEGACY_KIND_LABELS` apontando para `@/lib/admin/labels` fora do próprio `lib/admin/labels.ts`:
```bash
grep -rn "KIND_OPTIONS\|KIND_LABELS\|LEGACY_KIND_LABELS" src/ --include="*.tsx" --include="*.ts" | grep "lib/admin/labels"
```
Deve mostrar só a linha do re-export dentro do próprio `lib/admin/labels.ts`.

- [ ] **Step 5: Commit**

```bash
git add src/lib/imovel-kind-categories.ts src/lib/admin/labels.ts src/components/imovel/SearchFilterBar.tsx
git commit -m "refactor: mover labels de tipo de imóvel para módulo público (imovel-kind-categories)"
```

---

## Task 2: Metadados dinâmicos completos na ficha do imóvel

**Files:**
- Modify: `src/app/(site)/imovel/[slug]/page.tsx:27-38`

**Interfaces:**
- Consumes: `KIND_LABELS` de `@/lib/imovel-kind-categories` (Task 1), `formatPrice` de `@/lib/format`, `SITE` de `@/lib/site`.

- [ ] **Step 1: Substituir `generateMetadata`**

No topo do arquivo, adicione o import (junto aos outros imports de `@/lib/...`):
```ts
import { KIND_LABELS } from "@/lib/imovel-kind-categories";
import { formatPrice } from "@/lib/format";
```

Troque a função inteira (linhas 27-38):
```ts
export async function generateMetadata({
  params,
}: ImovelDetailPageProps): Promise<Metadata> {
  const { slug } = await params;
  const imovel = await getImovelBySlug(slug);
  if (!imovel) return {};

  const kindLabel =
    imovel.kind === "outros" && imovel.kindOther ? imovel.kindOther : KIND_LABELS[imovel.kind];
  const bedroomsFragment = imovel.bedrooms != null ? ` ${imovel.bedrooms} quartos` : "";
  const price = formatPrice(imovel.price, imovel.purpose);
  const title = `${kindLabel}${bedroomsFragment} no ${imovel.neighborhood}, ${imovel.city}/${imovel.state} — ${price} | ${SITE.name}`;
  const description =
    imovel.description.length > 160 ? `${imovel.description.slice(0, 157)}...` : imovel.description;
  const url = `${SITE.url}/imovel/${imovel.slug}`;

  return {
    title,
    description,
    alternates: { canonical: url },
    openGraph: {
      title,
      description,
      url,
      type: "website",
      siteName: SITE.name,
    },
  };
}
```

**Nota:** não defina `openGraph.images` aqui — a Task 3 cria `opengraph-image.tsx` colocado na mesma pasta, que o Next detecta automaticamente e injeta as tags `og:image`/`twitter:image` sozinho. Definir imagem nos dois lugares seria redundante.

- [ ] **Step 2: Verificar**

```bash
npm run build
```
Confirme que o título gerado bate com o formato do exemplo do plano ("Casa 3 quartos no Setor Sul, Itaberaí/GO — R$ 320.000 | Matias Imóveis") lendo o código — não há como renderizar e ver o `<title>` real sem navegador nesta sessão; documente isso como verificação pendente no relatório da task.

- [ ] **Step 3: Commit**

```bash
git add "src/app/(site)/imovel/[slug]/page.tsx"
git commit -m "feat(seo): metadados dinâmicos completos na ficha do imóvel"
```

---

## Task 3: Imagem Open Graph dinâmica por imóvel

**Files:**
- Create: `src/app/(site)/imovel/[slug]/opengraph-image.tsx`

**Interfaces:**
- Consumes: `getImovelBySlug` de `@/lib/queries`, `KIND_LABELS` de `@/lib/imovel-kind-categories` (Task 1), `formatPrice` de `@/lib/format`.
- Produces: rota especial `opengraph-image` — nenhum outro arquivo depende dela diretamente (o Next injeta as tags sozinho).

**Contexto:** usa `next/og` (`ImageResponse`), embutido no Next — nenhuma dependência nova. Para evitar qualquer busca de rede a fontes externas (Google Fonts) dentro da rota de imagem — algo que já se mostrou instável neste ambiente de sandbox ao longo desta sessão — a imagem usa a fonte padrão do sistema em vez de carregar Fraunces/Instrument Sans via `fetch`. É uma simplificação deliberada: prioriza a rota nunca falhar por causa de rede sobre a fidelidade tipográfica exata da imagem gerada.

- [ ] **Step 1: Criar `src/app/(site)/imovel/[slug]/opengraph-image.tsx`**

```tsx
import { ImageResponse } from "next/og";
import { getImovelBySlug } from "@/lib/queries";
import { KIND_LABELS } from "@/lib/imovel-kind-categories";
import { formatPrice } from "@/lib/format";

export const alt = "Foto do imóvel";
export const size = { width: 1200, height: 630 };
export const contentType = "image/png";

interface Props {
  params: Promise<{ slug: string }>;
}

// Imagem OG por imóvel: foto de capa (se houver) + selo de finalidade +
// preço + título, nas cores da marca. Usa a fonte padrão do sistema (sem
// fetch de fonte externa) de propósito — ver nota da Task 3 no plano.
export default async function Image({ params }: Props) {
  const { slug } = await params;
  const imovel = await getImovelBySlug(slug);

  if (!imovel) {
    return new ImageResponse(
      (
        <div
          style={{
            width: "100%",
            height: "100%",
            display: "flex",
            alignItems: "center",
            justifyContent: "center",
            background: "#161430",
            color: "#faf7f2",
            fontSize: 48,
          }}
        >
          Matias Imóveis
        </div>
      ),
      { ...size }
    );
  }

  const kindLabel =
    imovel.kind === "outros" && imovel.kindOther ? imovel.kindOther : KIND_LABELS[imovel.kind];
  const price = formatPrice(imovel.price, imovel.purpose);
  const badgeLabel = imovel.purpose === "locacao" ? "Locação" : "Venda";
  const badgeColor = imovel.purpose === "locacao" ? "#413e8c" : "#e6383d";

  return new ImageResponse(
    (
      <div
        style={{
          width: "100%",
          height: "100%",
          display: "flex",
          flexDirection: "column",
          justifyContent: "flex-end",
          position: "relative",
          background: "#161430",
        }}
      >
        {imovel.coverImage && (
          // eslint-disable-next-line @next/next/no-img-element
          <img
            src={imovel.coverImage}
            alt=""
            width={1200}
            height={630}
            style={{ position: "absolute", inset: 0, width: "100%", height: "100%", objectFit: "cover" }}
          />
        )}
        <div
          style={{
            position: "absolute",
            inset: 0,
            background: "linear-gradient(180deg, rgba(22,20,48,0.15) 0%, rgba(22,20,48,0.92) 100%)",
          }}
        />
        <div style={{ position: "relative", display: "flex", flexDirection: "column", padding: 64, gap: 16 }}>
          <div
            style={{
              display: "flex",
              alignSelf: "flex-start",
              background: badgeColor,
              color: "#ffffff",
              padding: "8px 20px",
              borderRadius: 999,
              fontSize: 28,
              fontWeight: 700,
            }}
          >
            {badgeLabel}
          </div>
          <div style={{ display: "flex", color: "#ffffff", fontSize: 56, fontWeight: 700, maxWidth: 1000 }}>
            {kindLabel} em {imovel.neighborhood}, {imovel.city}/{imovel.state}
          </div>
          <div style={{ display: "flex", color: "#faf7f2", fontSize: 44, fontWeight: 700 }}>{price}</div>
          <div style={{ display: "flex", color: "#e9e2d6", fontSize: 28 }}>Matias Imóveis · CJ-40079</div>
        </div>
      </div>
    ),
    { ...size }
  );
}
```

- [ ] **Step 2: Verificar**

```bash
npm run build
```
Confirme no output do build que a rota `opengraph-image` aparece listada para `/imovel/[slug]` sem erro de compilação. **Verificação pendente (sem navegador nesta sessão):** como a imagem renderiza de verdade — proporção do texto, se o `<img>` remoto carrega dentro do `ImageResponse`, se o degradê fica legível. Documente isso no relatório da task; não afirme que "está bonita" sem ter visto.

- [ ] **Step 3: Commit**

```bash
git add "src/app/(site)/imovel/[slug]/opengraph-image.tsx"
git commit -m "feat(seo): imagem Open Graph dinâmica por imóvel"
```

---

## Task 4: JSON-LD (RealEstateListing + BreadcrumbList) na ficha do imóvel

**Files:**
- Modify: `src/app/(site)/imovel/[slug]/page.tsx`

**Interfaces:**
- Consumes: os mesmos campos de `imovel` já desestruturados no componente da página (Task 2 não muda o componente, só `generateMetadata`).

**Contexto:** `title`/`description` hoje só existem dentro de `generateMetadata` (Task 2), não no componente da página — e o JSON-LD desta task precisa dos dois. Em vez de duplicar a lógica de montagem, esta task extrai `title`/`description` para duas funções compartilhadas e atualiza `generateMetadata` para usá-las também.

- [ ] **Step 1: Adicionar o import do tipo `Imovel`**

No topo do arquivo, junto aos outros imports de `@/lib/...`:
```ts
import type { Imovel } from "@/lib/types";
```

- [ ] **Step 2: Adicionar as funções compartilhadas de título/descrição**

Logo depois dos imports (antes de `interface ImovelDetailPageProps`), adicione:
```ts
function buildSeoTitle(imovel: Imovel): string {
  const kindLabel =
    imovel.kind === "outros" && imovel.kindOther ? imovel.kindOther : KIND_LABELS[imovel.kind];
  const bedroomsFragment = imovel.bedrooms != null ? ` ${imovel.bedrooms} quartos` : "";
  const price = formatPrice(imovel.price, imovel.purpose);
  return `${kindLabel}${bedroomsFragment} no ${imovel.neighborhood}, ${imovel.city}/${imovel.state} — ${price} | ${SITE.name}`;
}

function buildSeoDescription(description: string): string {
  return description.length > 160 ? `${description.slice(0, 157)}...` : description;
}
```

- [ ] **Step 3: Simplificar `generateMetadata` (Task 2) para usar as novas funções**

Substitua o corpo inteiro de `generateMetadata` (o que a Task 2 deixou) por:
```ts
export async function generateMetadata({
  params,
}: ImovelDetailPageProps): Promise<Metadata> {
  const { slug } = await params;
  const imovel = await getImovelBySlug(slug);
  if (!imovel) return {};

  const title = buildSeoTitle(imovel);
  const description = buildSeoDescription(imovel.description);
  const url = `${SITE.url}/imovel/${imovel.slug}`;

  return {
    title,
    description,
    alternates: { canonical: url },
    openGraph: { title, description, url, type: "website", siteName: SITE.name },
  };
}
```

- [ ] **Step 4: Montar o JSON-LD no componente da página**

No `ImovelDetailPage`, logo depois da desestruturação existente de `imovel` (depois da linha `} = imovel;`), adicione:
```tsx
  const title = buildSeoTitle(imovel);
  const description = buildSeoDescription(imovel.description);

  const jsonLd = {
    "@context": "https://schema.org",
    "@graph": [
      {
        "@type": "RealEstateListing",
        name: title,
        description,
        url: `${SITE.url}/imovel/${imovel.slug}`,
        image: imovel.coverImage ? [imovel.coverImage] : undefined,
        address: { "@type": "PostalAddress", addressLocality: city, addressRegion: state, addressCountry: "BR" },
        offers: {
          "@type": "Offer",
          price: imovel.price,
          priceCurrency: "BRL",
          availability: "https://schema.org/InStock",
        },
        numberOfBedroomsTotal: bedrooms,
        numberOfBathroomsTotal: bathrooms,
        floorSize: { "@type": "QuantitativeValue", value: areaM2, unitCode: "MTK" },
      },
      {
        "@type": "BreadcrumbList",
        itemListElement: [
          { "@type": "ListItem", position: 1, name: "Início", item: SITE.url },
          { "@type": "ListItem", position: 2, name: "Buscar imóveis", item: `${SITE.url}/imoveis` },
          { "@type": "ListItem", position: 3, name: title, item: `${SITE.url}/imovel/${imovel.slug}` },
        ],
      },
    ],
  };
```
(`city`, `state`, `bedrooms`, `bathrooms`, `areaM2` já existem na desestruturação atual do componente — não precisa redeclarar.)

- [ ] **Step 5: Renderizar o script**

Como primeiro filho de `<Container className="py-8">` (antes do `<Link href="/imoveis" ...>`), adicione:
```tsx
      <script
        type="application/ld+json"
        // eslint-disable-next-line react/no-danger
        dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }}
      />
```

- [ ] **Step 6: Verificar**

```bash
npm run build
```
Deve compilar sem erro de tipo. Rode também:
```bash
npm run lint
```
(o `eslint-disable-next-line react/no-danger` deve suprimir o aviso — se o projeto não tiver essa regra ativa, o comentário é inofensivo, apenas remova se o lint reclamar de comentário de supressão desnecessário).

**Verificação pendente (sem navegador/rede nesta sessão):** validar o JSON-LD gerado no [Google Rich Results Test](https://search.google.com/test/rich-results) só é possível com o site publicado — documente como pendente no relatório.

- [ ] **Step 7: Commit**

```bash
git add "src/app/(site)/imovel/[slug]/page.tsx"
git commit -m "feat(seo): JSON-LD RealEstateListing e BreadcrumbList na ficha do imóvel"
```

---

## Task 5: `sitemap.xml` e `robots.txt`

**Files:**
- Create: `src/app/sitemap.ts`
- Create: `src/app/robots.ts`

**Interfaces:**
- Consumes: `getAllPublishedSlugs` de `@/lib/queries`, `SITE` de `@/lib/site`.

- [ ] **Step 1: Criar `src/app/sitemap.ts`**

```ts
import type { MetadataRoute } from "next";
import { getAllPublishedSlugs } from "@/lib/queries";
import { SITE } from "@/lib/site";

// Páginas estáticas + uma entrada por imóvel publicado. Revalida a cada
// 60s, igual às páginas que usam os mesmos dados (ver `revalidate` em
// app/(site)/page.tsx e app/(site)/imovel/[slug]/page.tsx).
export const revalidate = 60;

export default async function sitemap(): Promise<MetadataRoute.Sitemap> {
  const slugs = await getAllPublishedSlugs();

  const staticRoutes: MetadataRoute.Sitemap = [
    { url: SITE.url, changeFrequency: "daily", priority: 1 },
    { url: `${SITE.url}/imoveis`, changeFrequency: "daily", priority: 0.9 },
    { url: `${SITE.url}/anuncie`, changeFrequency: "monthly", priority: 0.5 },
    { url: `${SITE.url}/contato`, changeFrequency: "monthly", priority: 0.5 },
    { url: `${SITE.url}/empresa`, changeFrequency: "monthly", priority: 0.5 },
    { url: `${SITE.url}/privacidade`, changeFrequency: "yearly", priority: 0.2 },
  ];

  const imovelRoutes: MetadataRoute.Sitemap = slugs.map((slug) => ({
    url: `${SITE.url}/imovel/${slug}`,
    changeFrequency: "weekly",
    priority: 0.8,
  }));

  return [...staticRoutes, ...imovelRoutes];
}
```

(A rota `/privacidade` referenciada aqui é criada na Task 9 — se você estiver executando as tasks fora de ordem, tudo bem: uma entrada de sitemap para uma página que ainda não existe não quebra o build, só fica "adiantada".)

- [ ] **Step 2: Criar `src/app/robots.ts`**

```ts
import type { MetadataRoute } from "next";
import { SITE } from "@/lib/site";

export default function robots(): MetadataRoute.Robots {
  return {
    rules: { userAgent: "*", allow: "/", disallow: "/admin" },
    sitemap: `${SITE.url}/sitemap.xml`,
  };
}
```

- [ ] **Step 3: Verificar**

```bash
npm run build
```
Confirme no output que `/sitemap.xml` e `/robots.txt` aparecem listados como rotas geradas, sem erro.

- [ ] **Step 4: Commit**

```bash
git add src/app/sitemap.ts src/app/robots.ts
git commit -m "feat(seo): sitemap.xml dinâmico e robots.txt"
```

---

## Task 6: Imagem Open Graph estática do site

**Files:**
- Create: `src/app/opengraph-image.tsx`

**Interfaces:** nenhuma — não depende de dados dinâmicos.

**Contexto:** cobre Home e todas as páginas públicas que não têm imagem própria (a ficha do imóvel, Task 3, tem prioridade sobre esta por estar mais específica na árvore de segmentos — é o comportamento padrão do Next, igual a `layout`/`metadata`). Usa o `logo-wordmark.png` já existente em `public/images/` via leitura de arquivo local (sem fetch de rede), seguindo o mesmo exemplo dos docs oficiais do Next para assets locais em `ImageResponse`.

- [ ] **Step 1: Criar `src/app/opengraph-image.tsx`**

```tsx
import { ImageResponse } from "next/og";
import { readFile } from "node:fs/promises";
import { join } from "node:path";

export const alt = "Matias Imóveis — Itaberaí/GO";
export const size = { width: 1200, height: 630 };
export const contentType = "image/png";

// Lida uma vez, em module scope, igual ao exemplo dos docs do Next para
// assets locais (o arquivo não depende de dados de request).
const logoData = await readFile(join(process.cwd(), "public/images/logo-wordmark.png"), "base64");
const logoSrc = `data:image/png;base64,${logoData}`;

export default function Image() {
  return new ImageResponse(
    (
      <div
        style={{
          width: "100%",
          height: "100%",
          display: "flex",
          flexDirection: "column",
          alignItems: "center",
          justifyContent: "center",
          gap: 32,
          background: "#161430",
        }}
      >
        {/* eslint-disable-next-line @next/next/no-img-element */}
        <img src={logoSrc} alt="" width={420} height={90} />
        <div style={{ display: "flex", color: "#faf7f2", fontSize: 36 }}>
          Imóveis em Itaberaí/GO e região
        </div>
      </div>
    ),
    { ...size }
  );
}
```

- [ ] **Step 2: Verificar**

```bash
npm run build
```
Confirme que a rota `opengraph-image` aparece para `/` sem erro de compilação (e que `public/images/logo-wordmark.png` realmente existe — se o nome do arquivo for diferente, ajuste o `join(...)` para o nome real antes de rodar).

- [ ] **Step 3: Commit**

```bash
git add src/app/opengraph-image.tsx
git commit -m "feat(seo): imagem Open Graph estática do site"
```

---

## Task 7: JSON-LD RealEstateAgent (site inteiro)

**Files:**
- Modify: `src/app/(site)/layout.tsx`

**Interfaces:**
- Consumes: `SITE` de `@/lib/site`.

- [ ] **Step 1: Adicionar o script ao layout público**

Conteúdo completo do arquivo:

```tsx
import type { ReactNode } from "react";
import { Navbar } from "@/components/layout/Navbar";
import { Footer } from "@/components/layout/Footer";
import { WhatsAppFab } from "@/components/layout/WhatsAppFab";
import { SITE } from "@/lib/site";

// RealEstateAgent no layout público inteiro (não em /admin) — presente em
// toda página pública para o Google associar qualquer página do site à
// mesma organização, não só a ficha de imóvel individual.
const organizationJsonLd = {
  "@context": "https://schema.org",
  "@type": "RealEstateAgent",
  name: SITE.name,
  url: SITE.url,
  telephone: SITE.phone,
  email: SITE.email,
  address: {
    "@type": "PostalAddress",
    streetAddress: SITE.address.street,
    addressLocality: SITE.address.city,
    addressRegion: SITE.address.state,
    addressCountry: "BR",
  },
  areaServed: `${SITE.address.city}/${SITE.address.state}`,
};

export default function SiteLayout({ children }: { children: ReactNode }) {
  return (
    <>
      <script
        type="application/ld+json"
        // eslint-disable-next-line react/no-danger
        dangerouslySetInnerHTML={{ __html: JSON.stringify(organizationJsonLd) }}
      />
      <Navbar />
      <main className="flex-1">{children}</main>
      <Footer />
      <WhatsAppFab />
    </>
  );
}
```

- [ ] **Step 2: Verificar**

```bash
npm run build && npm run lint
```
Ambos devem passar sem erro novo.

- [ ] **Step 3: Commit**

```bash
git add "src/app/(site)/layout.tsx"
git commit -m "feat(seo): JSON-LD RealEstateAgent em todas as páginas públicas"
```

---

## Task 8: `next/image` — `sizes` que faltava e blur no hero

**Files:**
- Modify: `src/components/imovel/ImovelGallery.tsx:45`
- Modify: `src/app/(site)/page.tsx` (import do hero + uso do `<Image>`)

**Interfaces:** nenhuma mudança de props/assinatura pública.

**Contexto:** achado já identificado (e propositalmente deixado pendente) na revisão final da branch do redesign — a foto principal da galeria não tem `sizes`, então cai no padrão de `ImovelPhoto` (`280px`) apesar de renderizar em ~760px de largura, uma imagem visivelmente mais "mole" que deveria. O hero da Home usa `src` como string (`/images/hero-house.webp`), o que não ativa o `placeholder="blur"` automático do Next — só funciona com import estático.

- [ ] **Step 1: Adicionar `sizes` na foto principal da galeria**

Em `src/components/imovel/ImovelGallery.tsx:45`, troque:
```tsx
        <ImovelPhoto src={coverImage} alt={title} iconClassName="w-12 h-12" priority />
```
por:
```tsx
        <ImovelPhoto
          src={coverImage}
          alt={title}
          iconClassName="w-12 h-12"
          priority
          sizes="(min-width: 1024px) 66vw, 100vw"
        />
```
(mesmo valor de `sizes` já usado em outros lugares do site para a coluna principal de 2/3 de largura em telas grandes — ver `ImovelCard.tsx`.)

- [ ] **Step 2: Converter o hero da Home para import estático com blur**

No topo de `src/app/(site)/page.tsx`, junto aos outros imports, adicione:
```ts
import heroImage from "../../../public/images/hero-house.webp";
```
(ajuste o caminho relativo se o arquivo estiver em outro lugar — confirme com `ls public/images/hero-house.webp` antes de escrever o import; o caminho acima assume que `page.tsx` está em `src/app/(site)/page.tsx` e a imagem em `public/images/hero-house.webp`, três `../` sobem até a raiz do projeto a partir de `src/app/(site)/`.)

Troque o uso do `<Image>` (dentro da `<section>` do hero):
```tsx
        <Image
          src="/images/hero-house.webp"
          alt="Fachada de um imóvel em Itaberaí/GO"
          fill
          priority
          sizes="100vw"
          className="object-cover -z-10"
        />
```
por:
```tsx
        <Image
          src={heroImage}
          alt="Fachada de um imóvel em Itaberaí/GO"
          fill
          priority
          placeholder="blur"
          sizes="100vw"
          className="object-cover -z-10"
        />
```
(`placeholder="blur"` só funciona com import estático — é por isso que o `src` virou `heroImage` em vez da string; o Next gera o blur automaticamente a partir do arquivo, sem nenhum trabalho extra.)

- [ ] **Step 3: Verificar**

```bash
npm run build
```
Confirme que compila sem erro de import (caminho relativo errado é o erro mais provável aqui — se o build reclamar de "module not found", ajuste o número de `../` no import do Step 2).

- [ ] **Step 4: Commit**

```bash
git add src/components/imovel/ImovelGallery.tsx "src/app/(site)/page.tsx"
git commit -m "fix(seo): sizes na foto principal da galeria + blur placeholder no hero"
```

---

## Task 9: Página de privacidade (LGPD) + aviso nos formulários

**Files:**
- Create: `src/app/(site)/privacidade/page.tsx`
- Modify: `src/lib/site.ts` (novo link em `FOOTER_LINKS`)
- Modify: `src/components/forms/ContactForm.tsx`
- Modify: `src/components/forms/SellForm.tsx`

**Interfaces:**
- Produces: rota pública `/privacidade`.

- [ ] **Step 1: Criar a página de privacidade**

```tsx
import type { Metadata } from "next";
import { Container } from "@/components/layout/Container";
import { SITE } from "@/lib/site";

export const metadata: Metadata = {
  title: "Política de privacidade",
  description: `Como a ${SITE.name} trata os dados de quem visita o site.`,
};

export default function PrivacidadePage() {
  return (
    <Container className="py-16 max-w-[720px]">
      <h1
        className="text-text-1 mb-6"
        style={{ font: "var(--text-display-md)", fontFamily: "var(--font-display)" }}
      >
        Política de privacidade
      </h1>

      <div className="flex flex-col gap-5 text-text-2" style={{ font: "var(--text-body-md)" }}>
        <p>
          Aqui explicamos, de forma direta, o que acontece com as informações de quem visita
          este site. Não usamos termos jurídicos genéricos — só o que realmente fazemos.
        </p>

        <div>
          <h2 className="text-text-1 mb-2" style={{ font: "var(--text-display-sm)", fontFamily: "var(--font-display)" }}>
            Formulários (Anuncie seu imóvel e Contato)
          </h2>
          <p>
            Quando você preenche um desses formulários e clica em &quot;Falar no WhatsApp&quot; ou
            &quot;Enviar mensagem&quot;, nós não guardamos nada no nosso servidor. O que
            acontece é: montamos uma mensagem com o que você escreveu e abrimos o WhatsApp para
            você enviá-la diretamente para nós. A partir daí, a conversa acontece no WhatsApp,
            sujeita à política de privacidade do próprio WhatsApp/Meta — não à nossa.
          </p>
        </div>

        <div>
          <h2 className="text-text-1 mb-2" style={{ font: "var(--text-display-sm)", fontFamily: "var(--font-display)" }}>
            Visitas ao site
          </h2>
          <p>
            Usamos o Vercel Analytics para entender, de forma agregada e anônima, quantas
            pessoas visitam o site e quais páginas são mais acessadas. Essa ferramenta não usa
            cookies de rastreamento nem identifica você individualmente.
          </p>
        </div>

        <div>
          <h2 className="text-text-1 mb-2" style={{ font: "var(--text-display-sm)", fontFamily: "var(--font-display)" }}>
            Dúvidas
          </h2>
          <p>
            Se tiver qualquer dúvida sobre como tratamos essas informações, fale com a gente
            pelo e-mail{" "}
            <a href={`mailto:${SITE.email}`} className="text-brand-primary no-underline">
              {SITE.email}
            </a>{" "}
            ou pelo telefone {SITE.phone}.
          </p>
        </div>
      </div>
    </Container>
  );
}
```

- [ ] **Step 2: Adicionar o link no rodapé**

Em `src/lib/site.ts`, dentro de `FOOTER_LINKS`, na coluna `"Empresa"`, adicione um item à lista `links` (depois de `"Contato"`):
```ts
  {
    heading: "Empresa",
    links: [
      { href: "/empresa", label: "Quem somos" },
      { href: "/contato", label: "Contato" },
      { href: "/privacidade", label: "Política de privacidade" },
    ],
  },
```

- [ ] **Step 3: Aviso curto no ContactForm**

Em `src/components/forms/ContactForm.tsx`, imediatamente antes do `<Button type="submit" ...>` final, adicione:
```tsx
      <p className="text-text-3" style={{ font: "var(--text-caption)" }}>
        Ao enviar, você será direcionado ao WhatsApp — não guardamos seus dados. Veja nossa{" "}
        <Link href="/privacidade" className="text-brand-primary no-underline">
          política de privacidade
        </Link>
        .
      </p>
      <Button type="submit" variant="primary" disabled={isSubmitting} className="w-full">
        Enviar mensagem
      </Button>
```
Adicione o import de `Link` no topo do arquivo, se ainda não existir:
```ts
import Link from "next/link";
```

- [ ] **Step 4: Aviso curto no SellForm**

Em `src/components/forms/SellForm.tsx`, imediatamente antes do `<Button type="submit" ...>` final, adicione o mesmo padrão:
```tsx
      <p className="text-text-3" style={{ font: "var(--text-caption)" }}>
        Ao enviar, você será direcionado ao WhatsApp — não guardamos seus dados. Veja nossa{" "}
        <Link href="/privacidade" className="text-brand-primary no-underline">
          política de privacidade
        </Link>
        .
      </p>
      <Button type="submit" variant="whatsapp" size="lg" disabled={isSubmitting} className="self-start mt-2">
        Falar no WhatsApp
      </Button>
```
Adicione o import de `Link` no topo do arquivo, se ainda não existir:
```ts
import Link from "next/link";
```

- [ ] **Step 5: Verificar**

```bash
npm run build && npm run lint
```
Confirme que `/privacidade` aparece como rota estática gerada no output do build. Confirme visualmente por leitura do JSX (sem navegador nesta sessão) que os dois avisos ficam bem posicionados — não escondidos, não competindo com o botão de envio.

- [ ] **Step 6: Commit**

```bash
git add "src/app/(site)/privacidade/page.tsx" src/lib/site.ts src/components/forms/ContactForm.tsx src/components/forms/SellForm.tsx
git commit -m "feat(lgpd): página de política de privacidade + aviso nos formulários"
```

---

## Depois da última task

Rode uma verificação final:
```bash
npm run lint
npm run build
```
Ambos devem passar limpos (mesma ressalva de sempre: a falha em "Collecting page data" para `/imovel/[slug]` só é esperada se o projeto Supabase estiver pausado nesse momento — confirme checando `mcp__plugin_supabase_supabase__get_project` antes de concluir que é isso, não assuma).

Depois, use a skill `superpowers:finishing-a-development-branch` para decidir o destino da branch `feature/seo-lgpd` (merge para `main`, PR, ou manter como está) — não faça merge por conta própria sem esse passo.
